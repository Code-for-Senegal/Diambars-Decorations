name: Diambars create README
description: Get contributor of Code for Senegal
env:
    TOKEN_SECRET: ${{secrets.TOKEN_SECRET}}
runs:
  using: node12 # Runtime Environment
  npm install
  npm start
  main: index.js # Script to run, path is relative